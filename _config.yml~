plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

highlighter: rouge
    
defaults:
  - scope:
      path: ""
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

collections:
  authors:
    output: true
